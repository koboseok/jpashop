# 테스트 코드 작성시 test > resources 생성 > .yml 파일을 가져와서 세팅
# 메모리상에 디비를 띄울 수 있다.

# 따로 설정 해주지 않아도 해당 디비로 알아서 메모리 디비로 잡아줌.. 레전드
spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 모드로 동작
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create # app 실행 시점에 create 전에 있던 데이터 다 날림
#    properties:
#      hibernate:
#       # show_sql: true # 운영단계에서는 사용하지 않는다. / systemout으로 찍힌다
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # 하이버네이트가 생성하는 SQL이 로그로 찍힌다.

